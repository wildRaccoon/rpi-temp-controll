# Основні залежності для програми контролю температури

# Керування розеткою Tapo
PyP100==0.1.0

# Датчики температури
w1thermsensor==2.0.0  # Для DS18B20 (1-Wire)
adafruit-circuitpython-max31855==1.4.0  # Для MAX31855 (SPI термопара)
spidev==3.6  # Альтернатива для SPI (якщо adafruit не працює)

# Веб-сервер та API
flask==3.0.0
flask-cors==4.0.0  # Для CORS підтримки

# Конфігурація
pyyaml==6.0.1

# Робота з датами та часом
python-dateutil==2.8.2

